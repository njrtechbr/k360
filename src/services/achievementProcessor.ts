
  }
}    }eturn 0;

      r, error);e:' lotmento em processaro nole.error('Er     consoror) {
 h (er    } catcks;
oclUnlta   return to
   adas`); desbloque conquistaslocks}alUntottal: ${le.log(`🎉 To  conso   
 
      }
cks;cks += unlolo   totalUn    
 tendantId);endant(atsForAttmentocessAchieveprit this.ks = awaunlocnst     coals) {
    ntsWithEvtendaf atdantId } o{ attent ns   for (co= 0;

   otalUnlocks   let t  
       });
ndantId']
 : ['atte   distinct     true },
 Id:antct: { attend sele         },
   }
    
         Dateson.end currentSealte:   ,
         on.startDatetSeas currente:        gata: {
              d where: {
({
       n.findManyaluatiosma.ev priithEvals = awaendantsWitatt      const l
ada atuas na temporom avaliaçõe catendentesr   // Busca
     }
n 0;
          retur
   ncontrada');a eorada ativenhuma temp Nole.log('❌ns        coSeason) {
(!current

      if  }); }
           
 ew Date() }e: { gte: n    endDat
       },w Date() { lte: neDate:    start      where: {
     {
   ndFirst(Season.ficationrisma.gamifi= await pntSeason curreconst 
      ada atualtemporBuscar  //   
     .');
    ntes..des atenra todos oas pado conquistcessanlog('🚀 Proconsole.ry {
      r> {
    tromise<numbe(): PAchievementsllcessAasync pro
  static )
   */to em loteessamenm proces (usado etendent todos os aistas para conqu  * Processa/**
 }

  ge;
  quiredAverage >= reverareturn a    ns.length;
luatiovaa, 0) / em + e.notsu => sum, e)duce((ons.re evaluatiaverage =const se;

     return falvaluations)th < minEations.leng(evalu    if ean {
r
  ): boolnumbealuations:    minEv, 
 umberdAverage: n requirey[], 
   ons: antiuavale(
    eeragAvHighic checkate statriv   */
  p
iações avalmínimo dea alta com  médi * Verifica
  }

  /**reak;
  dStuire>= reqk  maxStreaeturn
    r }
  }
   
    reak = 0;rrentSt     cuse {
   el   } );
   tStreakreak, currenmaxStath.max(xStreak = M  ma
      ;eak++ntStrre    cur= 5) {
     ==luation.nota(eva{
      if ions) evaluatof  evaluation stfor (con;

    ak = 0let maxStre    0;
 eak =tStrlet curren
    lse;
rn faeak) returedStr requigth <uations.lenalf (ev{
    i: boolean eak: number) requiredStrany[],luations: treak(evaeckFiveStarS static ch
  privateas
   */es 5 estrelde avaliaçõcia a sequênrific* Ve
  /**
   }
  }
  
  urn false;      retr);
rrotId}:`, eachievemenios para ${ar critéro verificr(`Erro aro console.er {
     ch (error)cat }
    } ;
     lse  return fat:
            defaul   
 
          lse; fa    return
      ':eason_winnerase 's    c:
    on'pinthly_cham  case 'mo      e)
ent automaticamsadas proceseciais (nãos espquista     // Con   
          );
s, 4.5, 50onEvaluationrage(seascheckHighAve this.return
          average_50': 'high_        case       
10);
   aluations, easonEvtreak(sarSkFiveStchecurn this.       ret0':
   k_1r_streae 'five_sta       cas         
 5);
 tions, sonEvaluaeatarStreak(seSs.checkFiv thi     return     ak_5':
reststar_e 'five_cas       
          = 10000;
 onXp > return seas       d_xp':
  n_thousanse 'te ca
       
           5000;seasonXp >=    return 
      nd_xp':housaive_t 'f   case
           ;
    nXp >= 1000easo return s
         _xp':se 'thousand ca
            ;
     = 100sonXp >sea    return     xp':
  dred_'hun     case             
100;
 >= uationCount eturn eval    r     ations':
 _evalue 'hundred     cas    
   0;
      nt >= 5luationCouurn eva      ret
    s':aluationifty_ev     case 'f
            10;
 Count >= n evaluation   retur':
       luationsevaten_ '    case          
  = 1;
  tionCount >urn evalua    ret    ':
  tionevaluarst_case 'fi       ) {
 evementIdh (achi   switc
    conquista dados no IDérios basearificar crit
      // Ve
).length;=== 5=> e.nota ilter(e s.fonEvaluati = seasoniveStarCount     const f, 0);
 oints || 0) (event.p +umnt) => seveeduce((sum, Events.rp = seasonXpeasonX   const s   th;
.lenguations= seasonEvalCount ationconst evalu);

        }
      }     }
          d
 nEneaso   lte: s        t,
 seasonStargte:           : {
          datendantId,
     atte       where: {
     any({
   .findMisma.xpEventit pr awants =easonXpEve     const s

 );
      }ata: 'asc' }y: { dorderB
              },    }
    End
    ason    lte: se
        easonStart,      gte: s     
 data: {
          ndantId,tte     a    ere: {
  wh({
       dManyfinion.ma.evaluatt prisawai = luations seasonEvast  con   
 orada atual tempcar dados da Bus    //
  e);
n.endDatsoseaw Date(nEnd = ne seaso      constate);
tDson.star(seat = new DateasonStarnst se  co  try {
    
  n> {booleae<): Promisny
     season: aring, 
 ementId: stiev
    ach ing,dantId: strattenteria(
    ementCri checkAchievtatic async
  private s   */da atual
 na temporanquistade uma coos itéri aos cre atendeendentse um aterifica   * V/**
   }

   0;
    }
rn
      retur);errotas:', isconquo de entcessamErro no proerror('   console.ror) {
   catch (er   } ;
 locksewUnurn n     retas`);
 bloqueaduistas dess conqovaocks} n${newUnlle.log(`🎉    conso  }

      }
      }
          }
               
 error);e}:`,ment.titliever ${achbloquea Erro ao des.error(`❌nsole  co        ) {
    nt')onstraiique c('Unncludese?.i.messagf (!error       i   
   any) {tch (error:        } caks++;
      newUnloc        );
e}`ment.titlhievea: ${acesbloqueadquista donole.log(`✅ C cons           });

         }
                ement.xp
 achieved:    xpGain            Date(),
  newdAt:  unlocke           n.id,
    rentSeasod: cursonI         sea       vement.id,
chieentId: a achievem            
   tendantId,at            {
          data:      ate({
   evement.crehia.unlockedAcprism   await {
                 try k) {
  ocldUnlf (shou   i

         );    tSeason
      curren  d, 
  evement.i      achi   d, 
 antI     attend    
 ntCriteria(chievemekA.chect thiswaick = adUnloonst shoul     c    }

   ada
    empora tda nestloquea; // Já desbtinue  con
         {t.id))hievemendIds.has(acunlocke (    if{
    ievements) of achent vemie (const ach  for
    da conquistarificar ca
      // Ve
0; = wUnlocksne let 
     vementId)); ua.achieua =>ments.map(edAchieveunlockt(w Se= neunlockedIds onst    c;

     })    }
       
 son.id currentSeasonId:   sea   ntId,
        attenda     {
    where:   ny({
   ent.findMaemedAchievrisma.unlock = await pmentsedAchieve unlockonst c
     rada atualdas na tempobloqueajá desonquistas Buscar c  // });

          rue }
tive: tac {      where:
   {Many(fig.findmentConma.achievet priss = awaimentt achieve
      conss ativaistasBuscar conqu      //    }

rn 0;
         retu;
  ada')iva encontratda empora Nenhuma tg('❌e.lo      consolson) {
  rrentSea    if (!cu
   });
    }
     
     }: new Date() gtendDate: {    e  ) },
     new Date(te: { lte:Da   start
       e: {      wherst({
  Fireason.findtionSificarisma.gamon = await prrentSeast cu
      consada atualmporBuscar te/   
      /   tId}`);
 : ${attendanra atendentepas uistaessando conq.log(`🏆 Procole   consry {
   
    tmber> {nug): Promise<antId: strintendendant(atntsForAttevemesAchiesc proc asyn static
 tual
   */porada ate na temndenara um ateamente pics automatnquistaa cocess* Pro*
   /*ssor {
  ementProceass Achiev clrt();

expoClientPrismaew a = nonst prism
c/client';
prisma} from '@ient Clmaport { Prisim